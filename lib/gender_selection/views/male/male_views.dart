import 'package:flutter/material.dart';

class MaleViews extends StatelessWidget {
  const MaleViews({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar());
  }
}
